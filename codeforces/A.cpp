#include <bits/stdc++.h>
using namespace std;
#define ll long long
// #include "../rin.h"

int main(){ 
    int x, n;
    cin >> x;
    int l = 0;
    multiset<int> m;
    m.insert(0);
    m.insert(x);
    vector<int> output;
    for(int i = 0; i < n; i++){
        
    }
}