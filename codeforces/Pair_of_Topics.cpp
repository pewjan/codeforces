#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define MOD 1000000007
#define for(i,n) for(int i = 0; i < n; ++i)
using namespace std;


int main(){
    int n;
    vector<int> t(n);
    vector<int> s(n);
    for(i,n) cin >> t[i];
    for(i,n) cin >> s[i];
}