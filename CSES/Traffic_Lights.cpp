#include <bits/stdc++.h>
using namespace std;
#include "../rin.h"
#define ll long long int


int main(){
    int x,n;
    cin >> x >> n;
    vector<int> q(n);
    for(int i = 0; i < n; i++) cin >> q[i];
    debug(q);
}